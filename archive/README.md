# Archive

This folder stores superseded or historical diagram files that are no longer the active published versions.

Confirmed duplicates are archived rather than deleted:

- `togaf-phase-f-architecture-change-management.*` was both duplicated and mislabelled; Architecture Change Management belongs to Phase H. The active pair remains under `docs/diagrams/governance/` with the Phase H filename.
- `togaf-stakeholder-map-views-concerns-v2.*` was a misplaced duplicate from `docs/diagrams/building-blocks/`. The active pair remains under `docs/diagrams/stakeholder/`.
